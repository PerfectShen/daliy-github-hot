name: Daily Stock Strategy & Record

on:
  schedule:
    # 北京时间 9:30
    - cron: '30 1 * * 1-5'
  workflow_dispatch:

# 【新增】必须给写入权限，否则无法保存 CSV
permissions:
  contents: write

jobs:
  run-stock-bot:
    runs-on: ubuntu-latest
    env:
      TZ: Asia/Shanghai
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
        
    - name: Install dependencies
      run: pip install -r requirements.txt
      
    - name: Run script
      env:
        FEISHU_WEBHOOK: ${{ secrets.FEISHU_WEBHOOK }}
        FEISHU_SECRET: ${{ secrets.FEISHU_SECRET }}
      run: python stock_bot.py
      
    # 【新增】将生成的 trade_history.csv 提交回仓库
    - name: Commit and Push CSV
      run: |
        # 配置 GitHub 机器人身份
        git config --global user.name "Stock Bot"
        git config --global user.email "bot@github.com"
        
        # 【关键修改】先检查文件是否存在
        if [ -f trade_history.csv ]; then
          echo "✅ 发现 CSV 文件，准备提交..."
          git add trade_history.csv
          
          # 检查是否有变动需要提交
          if git diff --staged --quiet; then
            echo "⚠️ 文件内容没有变化，跳过提交"
          else
            git commit -m "📈 Auto-update: Stock History Record"
            git push
            echo "🚀 推送成功！"
          fi
          
        else
          echo "⚠️ 今日无数据生成，未创建 trade_history.csv，跳过提交步骤。"
        fi
